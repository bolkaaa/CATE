# CATE

A corpus-based concatenative synthesis application written in C++ (work in progress). 

### Build Requirements
- portaudio
- portaudiocpp
- libsndfile
- boost/filesystem

### Building with CMake
mkdir build

cd build

cmake ..

make
